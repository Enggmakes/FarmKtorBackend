services:
  - type: web
    name: farm-ktor-backend
    env: docker
    plan: free
    region: frankfurt
    dockerfilePath: ./Dockerfile
    envVars:
      - key: FIREBASE_SERVICE_ACCOUNT
        sync: false
